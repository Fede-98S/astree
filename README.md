# Astree

Ici on va mettre le prototype à présenter à Fekri le 17 mars. 

Pour lancer, ce serait mieux d'utiliser un local host `python3 -m http.server` et d'ouvrire la page `http://localhost:8000` dans le navigateur pour ensuite naviguer le site.

